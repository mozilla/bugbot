The severity field for this bug is set to {{ extra[bugid]["severity"] }}. However, the `Performance Impact` field flags this bug as having a high impact on the performance.
:{{ nickname }}, could you consider increasing the severity of this performance-impacting bug? Alternatively, if you think the performance impact is lower than previously assessed, could you request a re-triage from the performance team by setting the `Performance Impact` flag to `?`?

{{ documentation }}
