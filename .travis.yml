language: python
env:
    - TOX_ENV=py36
python:
  - "3.6"

install:
    - pip install -r requirements-dev.txt

script:
    - flake8 auto_nag/ --exclude=auto_nag/tests/
    - tox -e $TOX_ENV

after_success:
    - coveralls

